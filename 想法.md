


爱上一项技术并四处寻找应用场景固然诱人，我也曾如此，每个人都曾如此。
但真正创造最大价值的工程师却反其道而行之：
他们专注于深入理解用户问题，并让解决方案从这种理解中自然而然地涌现。


### 修改

- [x] ai 润色按钮，不是改写，而是仅仅添加tags，把 ai 润色，改为添加 tags，功能已改
  - `gemini_service.py` 中 `add_tags_to_content` 已实现
  - `index.js` 中 toast 提示改为 "标签添加完成"
- [x] 发布进度并行处理，已使用 `ThreadPoolExecutor` 并行发布到多平台
- [x] 图片处理，缩小为 2K 大小，`app.py` 中 `resize_image_if_needed(save_path, max_size=2048)`
- [x] 发布取消按钮，已实现
  - `index.html` 中有取消按钮
  - `app.py` 中有取消 API `/api/publish/cancel/<job_id>`
- [x] 历史记录页面，已正常显示
- [x] 定时发布功能已删除
  - 删除 `app.py` 中的定时发布 API 和调度器
  - 删除 `models.py` 中的 `ScheduledPost` 模型
  - 删除 `templates/scheduled.html`
  - 删除 `index.js` 中的定时发布代码


- [x] 知乎的 hashtag 有问题的。最终我决定，知乎的内容，不加 hashtag!
  - 已在 `publisher_service.py` 中添加 `_remove_hashtags()` 函数，发布到知乎时自动过滤掉内容中的 hashtag


### 修改小事情
- [x] 请帮我检查细节问题，ui, 逻辑，是否都合适，是否需要稍微调整一下。请详细检查并处理
  - [x] 修复自检面板图标颜色不生效的问题
  - [x] 优化响应式布局，小屏幕下自检面板自动调整位置
  - [x] 添加文件类型和大小验证（限制20MB，支持png/jpg/jpeg/gif/webp/bmp）
  - [x] 改进知乎发布错误处理，确保浏览器正确关闭
  - [x] 添加Toast提示组件替代alert
  - [x] 修复删除最后一条历史记录后不显示空状态的问题
  - [x] 改进图片上传错误提示，显示具体失败原因


### 已完成
- [x] 初次启动，那些按钮的 icon 加载异常，能否下载到本地一个文件夹?
- [x] 对图片进行处理，如果大于 1080p，则缩小到 1080p。
- [x] 加快知乎发布的速度，减少流程，减少刷新。
- [x] 对 发布历史 进行管理，比如，可以清空，删掉。
- [x] 在发布页面，放一些很显著的提示，比如，
  - [x] 是否在讲道理，是否有帮助，是否有实际意义。
  - [x] 能否帮助别人节省时间。


- [x] 发布前自检，这里做成一个悬浮面板，可以拖放，放在文字编辑区域的左边。
- [x] 对这个项目进行重命名，尤其是首页的 title。起个好听的，简短的名字。
- [x] 增加一个图标，favicon.ico。
- [x] 导航栏增加一个关于页面，添加自己的社交链接，并写入readme 
  - x： https://x.com/fanchuangwater
  - github：https://github.com/buxuele
  - zhihu https://www.zhihu.com/people/fanchaung


### 2026-02-11 功能隐藏
以下功能已实现但隐藏（代码保留，UI 注释）：
- [x] 发布前自检面板 (quality-panel) - 可拖拽悬浮面板
- [x] 草稿箱 (draft-panel) - 多草稿管理，自动保存
- [x] 发布预览 (preview-modal) - 模拟 X/知乎 显示效果
- [x] 定时发布 (schedule-modal) - 排期列表，后台自动执行
- [x] 键盘快捷键提示 (shortcuts-hint) - Ctrl+Enter/S/L/P

**说明**: 所有功能代码完整保留在项目中，仅通过 HTML 注释隐藏了 UI 元素。详见 `隐藏功能文档.md`

### 推广任务
- 录个视频，演示效果。
- 写个文章，介绍这个项目。
- 开机自动启动。


### why flask
因为 node.js sucks. 无法调用系统命令。
